## 2026-04-26 - Inline Destructive Action Confirmation
**Learning:** Implementing inline confirmations for destructive actions using state (like clearing the cart) keeps users in context and avoids jarring browser dialogs.
**Action:** Use inline state-based confirmations with CSS transitions instead of native alert/confirm popups for destructive UI actions.
