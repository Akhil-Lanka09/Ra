## 2024-05-18 - Add confirmation dialog for delete action in cart
**Learning:** When implementing inline animated confirmations for destructive actions in React, relying on local component state (`useState`) and inline style transitions on existing UI classes avoids the need to inject raw CSS or new custom classes, strictly adhering to constraints while keeping the UI interactive. Screen reader clarity must be maintained by correctly assigning `aria-expanded` and explicit `aria-label` attributes to the newly introduced action buttons.
**Action:** Use inline React state combined with inline CSS transitions on existing button classes (`styles.clearBtn`) to swap out the "Clear all" button with an interactive "Yes/No" prompt. Ensure that any external testing scripts or screenshots are completely cleaned up and never added to git to prevent repository pollution.
